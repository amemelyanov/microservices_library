spring:
    application:
        name: rest-service
    profiles:
        active: dev
    cloud:
        config:
            uri: http://configserver:8071
    kafka:
        bootstrap-servers: localhost:9092